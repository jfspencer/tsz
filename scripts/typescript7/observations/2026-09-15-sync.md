# Repeatable full-suite synchronization

The new `scripts/typescript7/sync.py` command retains the full native source/setup
and nested corpus, verifies capture anchors, and reports exact file-level deltas
against a verified prior inventory. The production release remains 7.0.2.

Verification:

- `scripts/safe-run.sh python3 -m unittest discover -s scripts/typescript7 -p 'test_*.py'`:
  **40 tests pass**, including real Git transitions, dirty-input preservation,
  added/removed files, binary line endings, mode-only changes, inconsistent
  submodule pins, corrupted inventories, and changed capture anchors.
- Sync against the earlier full inventory: **136,383 unchanged files**, no
  additions, removals, or modifications. Repeating against the new sync report
  produces the same inventory and zero deltas.
- Inventory SHA-256:
  `4db2d6bda904025a4191df25cb65914652f93f363f90411ffc540722e98afb39`.
- Native numeric-separator smoke: **80 pass, one skip, 50 baseline products**.
  All 50 products remain byte-identical to the prior native observation.
  Captured compiler-input manifest remains
  `9b868a18399e02d28c887a1a99ae6be7e33d2b40498d0408b05a2714176903f8`.

Ignored artifacts: `artifacts/typescript7/7.0.2-sync/`,
`7.0.2-sync-repeat/`, and `7.0.2-sync-smoke/`.

Sync success proves materialization and inventory consistency. It does not
prove native suite success or TSZ parity. Production pin upgrades still need
an explicit coordinated update of active pins/consistency checks; changed
native APIs may require capture-adapter ports before the smoke passes.
